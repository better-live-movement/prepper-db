<%= form_with(model: storage_location, local: true) do |form| %>
  <% if storage_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(storage_location.errors.count, "error") %> prohibited this storage_location from being saved:</h2>

      <ul>
      <% storage_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :storage_location_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :storage_location_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
